let obj = {
    'chave1': 1,
    'chave2': 2,
    'chave3': 3,
}

console.log(obj);

//retorna um array cujo os elementos são strings correspondentes para a propriedade
console.log(Object.keys(obj));
